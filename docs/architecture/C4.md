# Arquitectura C4 de Dragon3

> Fuente: configuracion PM2 y rutas de produccion. Las cifras de rendimiento son objetivos o mediciones fechadas, no garantias universales.

## Nivel 1: contexto

```mermaid
C4Context
  title Contexto Dragon3
  Person(client, "Cliente", "Envía imágenes y consume veredictos explicables")
  System(dragon3, "Dragon3", "Analiza autenticidad visual mediante evidencias multimodales")
  System_Ext(mongo, "MongoDB", "Persistencia de resultados y metadatos")
  System_Ext(redis, "Redis", "Colas y coordinación de trabajos pesados")
  System_Ext(model, "Modelo ML", "Artefacto XGBoost externo al repositorio")
  Rel(client, dragon3, "Analiza una imagen", "HTTPS/JSON o multipart")
  Rel(dragon3, mongo, "Persiste resultados")
  Rel(dragon3, redis, "Publica/consume trabajos async")
  Rel(dragon3, model, "Carga mediante worker Python")
```

## Nivel 2: contenedores

```mermaid
C4Container
  title Contenedores de Dragon3
  Person(client, "Cliente")
  Container(backend, "Backend HTTP", "Node.js/Express/PM2 cluster", "Validación, autenticación, subida y adapter")
  Container(embassy, "Agent Embassy", "Node.js/Express/PM2 fork", "Defensa, selección de plan y orquestación")
  Container(engine, "Motor de células", "Módulos ES/CJS + planes JSON", "Evidencias y veredicto")
  Container(ml, "Worker ML", "Python persistente", "Predicción XGBoost por protocolo lineal")
  Container(watcher, "Dataset watcher", "Node.js/PM2 fork", "Procesamiento operativo en segundo plano")
  ContainerDb(mongo, "MongoDB", "Base de datos", "Resultados y metadatos")
  ContainerDb(redis, "Redis/Bull", "Cola", "Trabajos asíncronos")
  Rel(client, backend, "POST análisis")
  Rel(backend, embassy, "HTTP interno")
  Rel(embassy, engine, "Carga y ejecuta plan")
  Rel(engine, ml, "stdin/stdout JSON")
  Rel(embassy, mongo, "Persiste")
  Rel(embassy, redis, "Trabajos async")
  Rel(watcher, mongo, "Etiquetado dataset")
```

## Nivel 3: componentes del Embassy

```mermaid
flowchart TD
  A[HTTP Embassy] --> B[Autenticación y rate limit]
  B --> C[Selección de plan]
  C --> D[Orquestador]
  D --> E[Cargar imagen]
  D --> F[Celulas independientes]
  D --> G[Celulas dependientes]
  F --> H[Adapter de resultados]
  G --> H
  H --> I[Explicación cliente]
  H --> J[Persistencia y telemetría]
  D --> K[Worker ML]
  D -. async .-> L[Bull/Redis]
```

## Flujo de secuencia sincrono

```mermaid
sequenceDiagram
  participant C as Cliente
  participant B as Backend :3000
  participant E as Embassy :3002
  participant O as Orquestador
  participant M as Worker ML
  C->>B: multipart archivo
  B->>B: auth, MIME, tamaño, correlationId
  B->>E: execute(plan, archivo)
  E->>O: cargar plan
  O->>O: cargar-imagen
  par Señales independientes
    O->>O: EXIF/IA/forense/color/sombreado
    O->>M: predicción ML
  end
  O->>O: consistencia y veredicto
  O-->>E: resultados + tiempos
  E->>E: purga y adapter explicable
  E-->>B: respuesta normalizada
  B-->>C: veredicto + evidencias
```

## Propiedad y limites

| Componente | Ownership | Escalado actual | Estado |
|---|---|---:|---|
| Backend | HTTP/API | 2 cluster configurable | stateless salvo dependencias externas |
| Embassy | orquestación | 1 fork | mantiene worker ML y caches |
| Worker ML | inferencia | 1 por Embassy | proceso persistente |
| Dataset watcher | background | 1 fork | fuera del camino HTTP |
| MongoDB | persistencia | externo | requiere backup y monitorización |
| Redis/Bull | async | externo | no usar para hot path sincrono |

## Riesgos arquitectonicos abiertos

- El Embassy único es un punto de coordinación; multiplicarlo requiere diseñar ownership de worker ML, locks y caches.
- La ruta asincrona Bull debe exponerse mediante un contrato de job antes de usarse como API pública.
- Dev y prod son copias con riesgo de divergencia; cada release debe comparar el runtime generado.
